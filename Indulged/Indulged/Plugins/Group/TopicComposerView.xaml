<UserControl x:Class="Indulged.Plugins.Group.TopicComposerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:CS.Windows.Controls;assembly=CS.Windows.Controls"             
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480"
    Height="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Subject" Foreground="#448ccb" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox x:Name="SubjectTextBox"/>
        </StackPanel>

        <controls:WatermarkTextBox x:Name="SearchBox" Height="45"
                                   BorderThickness="0" Background="Transparent" Style="{StaticResource SearchBoxStyle}"
                                   Foreground="#448ccb" Margin="4" Padding="0" 
                                   WatermarkText="add some content..." AcceptsReturn="True"
                                   Grid.Row="1" />
    </Grid>
</UserControl>