<UserControl x:Class="Indulged.Plugins.Dashboard.UploadOptionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}" d:DesignWidth="480" Height="265">

    <StackPanel x:Name="LayoutRoot" Orientation="Vertical">
        <RadioButton x:Name="CameraButton" Content="{Binding Path=LocalizedResources.UploadOptionsCameraText, Source={StaticResource LocalizedStrings}}" 
                     GroupName="UploadGroup" Margin="20, 15, 20, 0" Style="{StaticResource FXRadioButtonStyle}"/>

        <RadioButton x:Name="LibraryButton" Content="{Binding Path=LocalizedResources.UploadOptionsLibraryText, Source={StaticResource LocalizedStrings}}" 
                     GroupName="UploadGroup" Margin="20, 8, 20, 0" Style="{StaticResource FXRadioButtonStyle}"/>
    </StackPanel>
</UserControl>