<UserControl x:Class="Indulged.Plugins.ProCam.Controls.EVDialControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock x:Name="Label" Grid.Column="0" VerticalAlignment="Center"
                   Foreground="White" />
        
        <Image x:Name="DialBaseView" Source="/Assets/ProCam/EVDial.png" 
               Width="90" Height="90" IsHitTestVisible="False"
               Grid.Column="1">
            <Image.RenderTransform>
                <RotateTransform x:Name="BaseRotateTransform" CenterX="45" CenterY="45"/>
            </Image.RenderTransform>
        </Image>
        
        <Canvas x:Name="GestureCaptureCanvas" Background="Black" Opacity="0" IsHitTestVisible="True"
                Grid.Column="1"/>
    </Grid>
</UserControl>